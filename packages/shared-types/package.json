{
  "name": "@indus/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Placeholder package. Shared TypeScript types/interfaces used across admin-web, vendor-mobile, and services/api (domain models, DTOs, enums). Fill in with real code — see src/index.ts.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
